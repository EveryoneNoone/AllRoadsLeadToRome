# ѕроект сервиса јвторизации и аутентификации пользователей

Ётот проект представл€ет собой микросервис авторизации, который использует ASP.NET Core и PostgreSQL дл€ обработки запросов аутентификации и авторизации.

## “ребовани€

ƒл€ запуска этого проекта вам понадоб€тс€:

- Docker

## —борка и «апуск

„тобы собрать и запустить контейнеры, выполните следующие шаги:

1. ќткройте терминал или командную строку.
2. ѕерейдите в директорию проекта, где находитс€ `docker-compose.yml`.
3. ¬ыполните команду:

```bash
docker-compose up --build
```

Ёта команда соберет образы дл€ всех сервисов, определенных в docker-compose.yml, и запустит их в контейнерах. ѕараметр --build указывает на необходимость пересборки образов, даже если они уже были собраны ранее.

ѕосле запуска сервисы будут доступны на следующих портах:
¬еб-приложение: http://localhost:8000
Ѕаза данных: localhost:5432 (ѕорт может быть изменен в соответствии с конфигурацией docker-compose.yml)
ќстановка и ”даление  онтейнеров
„тобы остановить работающие контейнеры и удалить их, воспользуйтесь следующей командой:

```bash
docker-compose down
```
Ёта команда остановит все сервисы и удалит контейнеры, созданные docker-compose up. Ёто не удалит созданные образы.